export const CurrentWeatherDetail = ({ currentWeather }) => {
    return (
        <section>
            <article>
                <p>DATOS DEL TIEMPO ACTUAL...</p>
            </article>
        </section>
    )
}